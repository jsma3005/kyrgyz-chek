import * as API from './api'
import * as Hooks from './hooks'
import { List } from './List'
import { Page } from './Page'

const Pages = {
  List,
  Page,
}

export const Orders = {
  Hooks,
  API,
  Pages,
}